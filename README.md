# Project Setup 
C'est le squelette, le setup, sur lequel on pourrait construire un projet react native.

Pour faire run les tests : 

npm install --save-dev jest @types/jest

npm install --save-dev @testing-library/react-native

npm run test